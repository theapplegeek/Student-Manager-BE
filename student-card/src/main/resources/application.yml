spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/student_card
        username: postgres
        password: Password01!
    jpa:
        hibernate:
            ddl-auto: create-drop
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true
        show-sql: true
server:
    error:
        include-message: always