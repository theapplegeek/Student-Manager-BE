spring:
  version: 2.6.6
  application:
    name: student-card
    version: 1.0
  profiles:
    active: ${spring.cloud.config.profile}
  cloud:
    config:
      uri: http://localhost:8888
      profile: dev
      label: main
      fail-fast: true
      retry:
        max-attempts: 10
